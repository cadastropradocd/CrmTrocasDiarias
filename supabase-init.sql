CREATE TABLE IF NOT EXISTS "User" (
  id TEXT PRIMARY KEY DEFAULT gen_random_uuid()::text,
  email TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL,
  name TEXT NOT NULL,
  "createdAt" TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS "Registro" (
  id TEXT PRIMARY KEY DEFAULT gen_random_uuid()::text,
  categoria TEXT UNIQUE NOT NULL,
  realizado DOUBLE PRECISION NOT NULL DEFAULT 0,
  meta DOUBLE PRECISION NOT NULL DEFAULT 0,
  "updatedAt" TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  "updatedBy" TEXT
);

INSERT INTO "Registro" (categoria, realizado, meta) VALUES
  ('AÇOUGUE', 5267.7, 3000),
  ('BAZAR/ELETRO/FLORES', 2803.23, 5000),
  ('PETSHOP', 933.87, 2000),
  ('BEBIDAS', 21429.61, 7000),
  ('FLC', 14087.31, 18000),
  ('HIGIENE', 4537.61, 3500),
  ('PADARIA', 1883.13, 4000),
  ('LIMPEZA', 3507.2, 3500),
  ('MERCEARIA', 50600.61, 47000)
ON CONFLICT (categoria) DO NOTHING;
